#include<stdio.h>
#include "Link.h"
#include "fun.h"
int main1() {
	int x = 7;
	int y = 7;
	define();
	/*
	*类函数宏的使用方法1
	*/
	OLD(x)
		NEW(x)
		OLD(y)
		NEW(y)
		/*
		*关于##的使用方法
		*/
		int NUM(1) = 188;
	printf("%d", x1);

	//*************************（变参的实现）*************************
	P(使用的还行吧);
	P(%d, 25);
	SP(x = %d\ny = %d\n, x, y)
		//****************************************************************************
		MAX(55, 25)
		return 0;
}